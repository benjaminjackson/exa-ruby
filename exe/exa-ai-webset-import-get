#!/usr/bin/env ruby
# frozen_string_literal: true

require "exa-ai"

# Parse arguments
import_id = nil
api_key = nil
output_format = "json"

args = ARGV.dup
while args.any?
  arg = args.shift
  case arg
  when "--api-key"
    api_key = args.shift
  when "--output-format"
    output_format = args.shift
  when "--help", "-h"
    puts <<~HELP
      Usage: exa-ai webset-import-get <import_id> [OPTIONS]

      Get details of a specific import

      Arguments:
        import_id              ID of the import (required)

      Options:
        --api-key KEY          Exa API key (or set EXA_API_KEY env var)
        --output-format FMT    Output format: json, pretty, or text (default: json)
        --help, -h             Show this help message

      Examples:
        exa-ai webset-import-get imp_123
        exa-ai webset-import-get imp_123 --output-format text
    HELP
    exit 0
  else
    # First positional argument is import_id
    if import_id.nil?
      import_id = arg
    else
      $stderr.puts "Unknown option: #{arg}"
      exit 1
    end
  end
end

# Validate
if import_id.nil?
  $stderr.puts "Error: import_id argument is required"
  $stderr.puts "Usage: exa-ai webset-import-get <import_id> [OPTIONS]"
  $stderr.puts "Try 'exa-ai webset-import-get --help' for more information"
  exit 1
end

begin
  # Resolve API key and format
  api_key = Exa::CLI::Base.resolve_api_key(api_key)
  output_format = Exa::CLI::Base.resolve_output_format(output_format)

  # Build client
  client = Exa::CLI::Base.build_client(api_key)

  # Get import
  import = client.get_import(import_id)

  # Format and output
  output = Exa::CLI::Formatters::ImportFormatter.format(import, output_format)
  puts output

rescue Exa::NotFound => e
  $stderr.puts "Import not found: #{e.message}"
  exit 1
rescue Exa::Unauthorized => e
  $stderr.puts "Authentication error: #{e.message}"
  $stderr.puts "Check your API key (set EXA_API_KEY or use --api-key)"
  exit 1
rescue Exa::ClientError => e
  $stderr.puts "Client error: #{e.message}"
  exit 1
rescue Exa::ServerError => e
  $stderr.puts "Server error: #{e.message}"
  exit 1
rescue Exa::Error => e
  $stderr.puts "Error: #{e.message}"
  exit 1
rescue StandardError => e
  $stderr.puts "Unexpected error: #{e.message}"
  $stderr.puts e.backtrace.first(5) if ENV["DEBUG"]
  exit 1
end
